Put your `.vrm` files here.

This directory is gitignored: a VRoid export bundles pixiv's built-in hair,
clothing and accessory assets, which carry their own licence terms separate from
your rights to the character. Keep models out of version control.
